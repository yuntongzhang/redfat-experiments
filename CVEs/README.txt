CVEs with non-incremental overflows

Relevant portions of code have been put together into the driver programs.

Commands to build and run the program with exploit input are shown in the comments in each driver file.
